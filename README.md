# AI 编程怎么真正跑起来

现场页，打开就能投屏。

**现场：[https://ceastld.github.io/ai-coding-talk/](https://ceastld.github.io/ai-coding-talk/)**

讲的是流程，不是模型：规则写进仓库，Agent 按文件干活。

- 方向键 / 空格翻页。走单页先揭步骤，再进下一张
- `F` 全屏 · `P` 备注
- 地址带页码，`#/11` 直接进三张假工单
- 源材料在 `talk/`，决策表和 skill 在 `examples/mini-automation/`
- 仓库里没有密钥。公开只是为了 GitHub Pages

本地预览：

```bash
python -m http.server --directory docs 4173
```
