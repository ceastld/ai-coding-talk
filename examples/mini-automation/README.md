# 迷你 automation（演示用）

缩小版的 `qk-automation`。没有监控、没有真的 GitHub 写入。
现场只读这些文件，按决策表口头走三条假单（见 `../../talk/demo-script.md`）。

Agent 如果拿本目录当真规则：先读 `AGENTS.md` 和 `policies/decision-rules.md`，再打开匹配的 `skills/<name>/SKILL.md`。
